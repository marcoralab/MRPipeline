# snakemake -s mr.smk --configfile test.yaml --keep-going --until Harmonize
# snakemake -s mr.smk --configfile test.yaml --dag | dot -Tsvg > dag_mr.svg
# snakejob -s mr.smk --configfile test.yaml -j 1000 --max-jobs-per-second 1 --keep-going --until ProxySnps

DataIn: '1_RawData/GWAS/'
DataOut: "2_DerivedData/"

REF: '1_RawData/EUR_All_Chr'
traits: '1_RawData/MRTraits.csv'
Pthreshold: ['5e-8', '5e-6']

## LOAD as exposure
ExposureCode: ['alcc', 'alcd', 'audit']
OutcomeCode: ['ggt', 'alp', 'alt']

missing : [
{"ExposureCode" : "alcd", "OutcomeCode" : "ggt", "Pthreshold" : "5e-8"},
{"ExposureCode" : "alcd", "OutcomeCode" : "alp", "Pthreshold" : "5e-8"},
{"ExposureCode" : "alcd", "OutcomeCode" : "alt", "Pthreshold" : "5e-8"},
 ]
