# snakemake -s mr.smk --configfile test.yaml --keep-going --until ProxySnps
# snakemake -s mr.smk --configfile test.yaml --dag | dot -Tsvg > dag_mr.svg
# snakejob -s mr.smk --test.yaml -j 1000 --max-jobs-per-second 1 --keep-going --until ProxySnps

DataIn: '1_RawData/GWAS/'
DataOut: "2_DerivedData/"

REF: '1_RawData/EUR_All_Chr'
traits: '1_RawData/MRTraits.csv'
Pthreshold: ['5e-6', '5e-8']

## LOAD as exposure
ExposureCode: ['load', 'npany', 'npord', 'nft4', 'nftord', 'hips']
OutcomeCode: ['ldl'] 
